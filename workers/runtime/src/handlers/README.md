# Worker handlers

This directory contains registered worker handlers for the runtime.

Handlers should be small, deterministic functions that execute business logic with tenant context and submit actions through the substrate.

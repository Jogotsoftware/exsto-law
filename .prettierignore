node_modules
dist
.next
build
coverage
pnpm-lock.yaml
.turbo
**/*.md
*.yaml
*.yml

# Vendored third-party agent-skills (managed by skills-lock.json) — not our code to format.
.agents
# Vendored design export — byte-exact source of truth, never reformat
docs/design/legal-instruments/
# Build artifact: copy-pdf-worker.mjs drops the minified pdfjs worker here
# (git-ignored); without this, format:check fails locally after any app build.
apps/legal-demo/public/pdf-worker/
